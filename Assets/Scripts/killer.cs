using UnityEngine;
using System.Collections;

public class killer : MonoBehaviour {
    //kill people in <room> at <time> iff <numPeople> < 3 else pick another room
    //if player is speaking to the killer by themselves what happens?
    // solution is to make at <time> each character do something.

    int seed;

    //If killer is on same floor one person dies
    //
    
	// Use this for initialization
	void Start () {
	    
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
